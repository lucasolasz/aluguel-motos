# Perfil DEV — somente COMPORTAMENTO. Valores/segredos vem do .env (ver .env.example).
spring.jpa.hibernate.ddl-auto=create-drop
# show-sql imprime SQL com placeholders '?' (sem valores). LGPD: NAO habilitar
# logging.level.org.hibernate.orm.jdbc.bind=TRACE — isso vazaria CPF/RG/CNH nos logs.
spring.jpa.show-sql=true
spring.jpa.defer-datasource-initialization=true
spring.sql.init.mode=always
